# code: language=ansible
---

# 00 # Always

always_packages:
  - "dnf-utils"
  - "python3-libselinux"

# 08 # Locale

common_locale_packages:
  - "glibc-common"
  - "glibc-all-langpacks"

common_locale_config: "/etc/locale.conf"
