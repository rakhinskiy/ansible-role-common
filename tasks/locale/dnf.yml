# code: language=ansible
---

- name: "Common | Locale | Install dependencies | Dnf"
  ansible.builtin.dnf:
    name: "{{ common_locale_packages | default(__common_locale_packages__) }}"
    state: present
    update_cache: true
  become: true
  tags:
    - common
    - common-locale
